How you can describe a monitor?|An object that contains the current non-negative value and the queue of suspended processes.|An object that contains only the queue of suspended processes.|An object that contains only the counter of available resources.|A special module that contains shared resources and provides methods (functions) that operate on these resources.|D
Which sentence describes a deadlock?|Deadlock always occurs when the process tries to claim shared resource.|Deadlock is a state when two or more processes do not terminate, because they cannot continue their action due to waiting for each other.|Deadlock never occurs when a shared resources are protected by mutexes.|Deadlock always occurs when the number of available resources is less than the number of processes.|B
What is a property of the network model?|Model where the processors are connected by connection network and communication between processors is done by the shared memory.|Model where the processors are connected by connection network and communication between processors is done by message passing by this network.|Model where the processors are connected by connection network, where the number of processors is equal to the power of 2.|Model where memory blocks are connected by the network.|B
What assumption is made in the Amdahl's law?|Solving in parallel a problem of a fixed size.|A fixed number of processors communicating using shared memory.|The number of available processors is greater than the size of input data.|The number of available processors is equal to the power of 2.|A
How you can describe a semaphore?|A synchronization object that contains the current non-negative value and the queue of suspended processes.|A synchronization object that contains only the queue of suspended processes.|A synchronization object that contains only the number of available resources.|A synchronization object that contains shared resources and provides methods (functions) that operate on these resources.|A
How is the speedup S(p,n) of a parallel algorithm defined?|S(p,n) = n|S(p,n) = T(1,n)/T(p,n)|S(p,n) = p|S(p,n) = p * T(p,n)|B
How you can describe lock(m) operation performed on mutex m?|It returns true if the current status of the mutex m is locked. It suspends the process when the current status of the mutex m is unlocked.|If the current status of the mutex m is locked, it suspends the process and inserts it into the queue associated with the mutex m, otherwise (the state is unlocked) it changes its state to locked and the process is not suspended.|It only changes the mutex m to the locked state.|It changes the status of the mutex m to the opposite.|B
What is an optimal value of efficiency of parallel algorithm E(p,n) = S(p,n)/p?|1|p|n|p*n|A
What is a property of PRAM model?|Model where potentially infinite number of processors have access to a single unbounded shared memory.|A model in which only the program is stored in RAM but the data is stored in disk memory.|A model in which the only way of communication between processors is message passing by the connection network.|All answers are correct.|A
What will be the value of variable x after executing two processes (int x=0, both do x=x+1)?|Always x = 0.|Always x = 2.|Always x = 1.|It is undefined.|D
What assumption is made in the Gustafson-Barsis's law?|Maintaining a fixed execution time by increasing the size of a solved problem.|The number of available processors is equal to size of input data.|The size of input data is equal to the power of 2.|The number of available processors is equal to the power of 2.|A
What is a property of SIMD model?|It enables parallel data processing.|It is an asynchronous model - processing units perform different operations on different data.|It is a multiprocessor computer.|Different instructions must be performed on the same data at any given time.|A
How you can describe a condition variable?|A synchronization object that contains only the queue of suspended processes.|A synchronization object that is created in a locked state.|A synchronization object that can only take the values 0 or 1.|A synchronization object on which only one process can be suspended.|A
Given is a general semaphore S with an initial value of 0. Select the correct answer.|Performing a V(S) operation by the process will suspend its execution.|Performing a P(S) operation by the process will suspend its execution.|Performing the P(S) operation by the process will decrease the value of the semaphore by 1.|Performing the V(S) operation by the process will lead to deadlock.|B
How you can describe a mutex?|A synchronization object that contains the current status (unlocked, locked) and the queue of suspended processes.|A synchronization object that contains only the queue of suspended processes.|A synchronization object that contains only the counter of available resources.|A synchronization object that contains shared resources and provides methods (functions) that operate on these resources.|A
What is a critical section?|A part of code where that accesses shared resources by processes executed simultaneously and the resource is modified.|A part of code where a fatal error reported by the operating system occurs.|A part of code where all shared resources are declared.|A part of code where we create all processes executed in parallel.|A
Which architecture, according to Flynn's taxonomy, represents a multiprocessor computer?|MIMD|SIMD|none of the listed|SISD|A
What is the execution of processes with time sharing?|Merging all processes into one.|Processes are assigned to different processors. All operations from the first process are performed first, then from the second process, and so on until all operations are performed.|Processor time is divided to perform operations on several processes. Processes assigned to the same processor are executed in sequential - pseudo-parallelism.|The dynamic creation of new processes depending on the number of processors.|C
What is the optimal value of parameter S(p,n) = T(1,n)/T(p,n) which defines a speedup of parallel algorithm?|n|1|1/p|p|D
Which of the conditions must be satisfied for all possible executions to ensure the safety of process?|The number of processes executed in parallel is not greater than 3.|Process doesn't perform time-consuming calculations.|Two processes execute simultaneously the same critical section.|The deadlock never occurs.|D
What is the initial value of the semaphore after creating it?|Always 1.|Always 0.|Always maximum.|A user-defined value.|B
Which synchronization object is used inside a monitor?|barrier synchronization|binary semaphore|semaphore|condition variable|D
What is the base of computer architectures classification in Flynn's taxonomy?|The speed of data transfer between processors.|The number of instruction streams and data streams available in the architecture.|A way to access memory by processors.|The number of available processors, breakdown into: sequential computers, supercomputers and hypercomputers.|B
What is the result of performing the wait(c) operation on the condition variable c?|It returns the number of suspended processes on the condition variable c.|If the current state of the condition variable c is locked, it suspends the process, otherwise it is an empty operation.|It suspends the execution of the process for the number of milliseconds defined for the condition variable c.|It suspends the current process and put it into the queue of suspended processes.|D
How is the cost C(p,n) of a parallel algorithm defined?|C(p,n) = T(1,n) - T(p,n)|C(p,n) = n * T(1,n)|C(p,n) = p * T(p,n)|C(p,n) = T(1,n)/T(p,n)|C
What does the FLOPS unit defines?|The maximum time a process can be stopped on a semaphore.|A measure of computer performance.|The speed of reading data from the RAM.|System bus bandwidth connecting the GPU to the CPU.|B
Select the correct answer about mutex.|We can not use mutex for the protection of the critical section.|During creation, the user must to define the initial state of the mutex by himself.|A mutex can represent a counter of available resources.|At a given time a mutex can be locked by at most single process.|D
How to describe the starvation?|A state when the process performs time-consuming calculations.|A state when two or more processes do not terminate, because they cannot continue their action due to waiting for each other.|It is a state when a process is perpetually denied necessary resources to process its work.|A state when the number of shared resources is less than the number of processes.|C
How you can describe P(S) operation performed on semaphore S?|It returns the current value of semaphore s.|If the current value of semaphore s is 0 then it suspends process, otherwise it decrements the value of semaphore s.|It returns the number of suspended processes on the semaphore.|It suspends process unconditionally.|B
How you can describe unlock(m) operation performed on mutex m?|It resumes execution of all suspended processes on the mutex m.|It returns true if the current status of the mutex m is unlocked.|It only changes the mutex m to unlocked state.|None of the answers are correct.|D
What is the result of performing the wait(c) operation on the condition variable c?|It suspends the execution of the process for the number of milliseconds defined for the condition variable c.|It returns the number of suspended processes on the condition variable c.|It suspends the current process and put it into the queue of suspended processes.|If the current state of the condition variable c is locked, it suspends the process, otherwise it is an empty operation.|C
How you can describe V(S) operation performed on semaphore S?|It resumes execution of all suspended processes on the semaphore S.|It returns the number of suspended processes on the semaphore S.|It increments value of the semaphore S, if there are no suspended processes on this semaphore, otherwise it resumes first suspended process.|It sets initial value of semaphore S.|C
Which of the following parameters is not used to evaluate a connection network in the network model?|maximum degree|area of the network|cost|diameter|B
What can you say about the blocked-set semaphore S?|The V(s) operation performed on this semaphore resumes execution of all suspended processes.|A synchronization object that contains only the set of suspended processes.|The minimum value of this semaphore is undefined.|Its use may starve the process.|D
How to describe the liveness of process?|It is the state when the process is inside the monitor.|It is a state when the parent process waits until its child process finish its execution.|It is the state when the process is currently executing the critical section.|It defines the conditions that should be satisfied for all possible executions in a finite time.|D
